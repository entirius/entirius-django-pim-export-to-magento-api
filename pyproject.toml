[project]
name = "entirius-django-pim-export-to-magento-api"
version = "2.1.0"
description = "Products, prices, pictures, and attributes export from PIM to Magento via the Magento REST API"
authors = [{ name = "Entirius" }]
readme = "README.md"
requires-python = ">=3.11"
license = "MPL-2.0"
dependencies = [
    "django>=5.0",
    "celery>=5.4",
    "celery-once>=3.0.1",
    "python-slugify",
    "tqdm",
    "entirius-py-idx-normalizator>=2.0.0",
    "entirius-py-magento2-sdk2>=2.0.0",
    "entirius-py-process-logger>=2.0.0",
    "entirius-django-pim>=3.0.0",
    "entirius-django-pricemanager>=4.0.0",
    "entirius-django-regional>=2.0.1",
    "entirius-django-utils>=2.0.0",
]

[project.optional-dependencies]
dev  = ["ruff", "pre-commit"]
test = ["pytest", "pytest-django", "psycopg[binary]"]

[project.urls]
Repository = "https://github.com/entirius/entirius-django-pim-export-to-magento-api"

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src/django_pim_export_to_magento_api"]

[tool.ruff]
line-length = 120
target-version = "py311"
src = ["src", "tests"]

[tool.ruff.lint]
select = ["E", "W", "F", "I", "B", "C4", "UP", "DJ", "S"]
ignore = ["E501"]

[tool.ruff.lint.per-file-ignores]
"__init__.py" = ["F401"]
"tests/**" = ["S101", "S105"]
# Legacy migrated 1:1 — pre-existing lint debt, tracked outside this repo; do not grow these lists.
"src/django_pim_export_to_magento_api/*.py" = ["UP031"]
"src/django_pim_export_to_magento_api/models/*" = ["DJ001", "DJ008", "DJ012"]

[tool.ruff.lint.isort]
known-first-party = ["django_pim_export_to_magento_api"]

[tool.ruff.format]
quote-style = "double"

[tool.pytest.ini_options]
DJANGO_SETTINGS_MODULE = "tests.settings"
testpaths = ["tests"]
pythonpath = ["."]

[tool.deptry]
pep621_dev_dependency_groups = ["dev", "test"]

[tool.deptry.per_rule_ignores]
# Runtime imports are TYPE_CHECKING-only (FKs reference django_regional models by string), but the
# squashed migration depends on django_regional.0002 — the >=2.0.1 constraint must stay explicit.
DEP002 = ["entirius-django-regional"]
